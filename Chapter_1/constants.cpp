#include <iostream>

using namespace std;

int main() {

    const double conversion  = 2.54; //since the conversion does not show never, we just keep it constant just in case if someone tried to change it, compiler warns them not to because it is constant
    const int num_of_students = 20; //testing

    

    return 0;
}

